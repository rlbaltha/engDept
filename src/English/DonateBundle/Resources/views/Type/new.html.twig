{% extends 'EnglishDonateBundle:Donate:layout.html.twig' %}
 
{% block body %}

<form action="{{ path('type_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>


    
        <a href="{{ path('type') }}">
            Back to the list
        </a>
    
{% endblock %}
