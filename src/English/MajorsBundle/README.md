Notes on MajorsBundle
========================

The Hours entity is for importing data; the join is on can (a unique id in both tables)
The doctrine generator fails to write the keys correctly so should be omitted