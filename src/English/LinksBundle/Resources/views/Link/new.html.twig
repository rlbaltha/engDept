{% extends 'EnglishLinksBundle::subnav.html.twig' %}

{% block content %}
        <form action="{{ path('link_create') }}" method="post" {{ form_enctype(form) }} novalidate="novalidate">
            {{ form_widget(form) }}
                <button type="submit">Create</button>
        </form>
{% endblock %}

